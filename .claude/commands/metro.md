---
description: Start Metro bundler for React Native
allowed-tools: Bash(npm:*)
---

Start the Metro bundler for React Native development.

Run: `npm start`

Options:
- Press `a` to run on Android
- Press `r` to reload
- Press `d` to open developer menu
